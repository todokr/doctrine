# PROTOTYPE（使い捨て）— ② レビューアプリの画面構成

issue #17 の検討で作った UI プロトタイプ。**このブランチはマージしない。**
決定は `docs/superpowers/specs/2026-09-13-review-app-design.md` にある。

- `review-app-ui.html` — ブラウザで直接開ける単一 HTML。データはモック、操作はメモリ上のスタブ
- 確かめたこと: サイドバー（区分つきタスク一覧）＋中央（レビュー画面／タスク画面）という構成
- **注意: この版は spec で採らなかった「見た」印（ファイルごとの印・`v` キー・未確認ファイルの確認ダイアログ）を含む。**
  また spec で決めた「経緯」の欄は入っていない。画面の構成だけを参照すること
- 検証は JavaScript の構文チェックのみ
- 経緯: カンバン（列＝ステップ）→ 受信箱＋パイプライン行 → 本版、の順に作り直した。前の2版はこの HTML の過去の姿で、
  spec 13章に採らなかった理由を残している
